name: Update Top Languages Card

on:
  push:
    branches:
      - main
      - master
    paths:
      - ".github/workflows/update-top-langs.yml"
      - "scripts/generate_top_langs_svg.py"
  schedule:
    - cron: "17 3 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-card:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Generate SVG
        env:
          PROFILE_USERNAME: dario-gms
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python scripts/generate_top_langs_svg.py \
            --username "$PROFILE_USERNAME" \
            --output assets/top-langs-cyberpunk.svg \
            --top 5

      - name: Commit and push if changed
        run: |
          if git diff --quiet -- assets/top-langs-cyberpunk.svg; then
            echo "No changes in top-langs card."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/top-langs-cyberpunk.svg
          git commit -m "chore: update top languages card"
          git push
